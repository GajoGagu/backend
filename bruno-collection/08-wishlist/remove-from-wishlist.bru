meta {
  name: Remove from Wishlist
  type: http
  seq: 3
}

delete {
  url: http://localhost:8000/wishlist/items/{{wishlist_item_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response confirms removal", function() {
    expect(res.getBody().message).to.include("삭제");
  });
}
