meta {
  name: Get Categories Tree
  type: http
  seq: 3
}

get {
  url: http://localhost:8000/categories/tree
  body: none
  auth: none
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response is an array", function() {
    expect(res.getBody()).to.be.an('array');
  });
  
  test("Categories have children", function() {
    if (res.getBody().length > 0) {
      expect(res.getBody()[0]).to.have.property('children');
    }
  });
}
